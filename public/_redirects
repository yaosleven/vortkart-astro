# Legacy path safety net (about page was merged into /factory)
# NOTE: www -> non-www canonical redirect is handled by Cloudflare
# dashboard Redirect Rules after binding the custom domain (Cloudflare
# _redirects only allows relative-path targets, not absolute URLs).
/about        /factory   301
/about/       /factory   301
